# Support Vector Machine
#
# TODO: implement from scratch with NumPy.
# Finds the maximum-margin separating hyperplane.
# Start with the linear primal using hinge loss and gradient descent,
# then optionally add the kernel trick for nonlinear boundaries.
