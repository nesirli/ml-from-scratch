# Attention Mechanism
#
# TODO: implement from scratch with NumPy.
# Computes a weighted sum of values where the weights come from scaled
# dot-product similarity between queries and keys.
# Apply softmax to the scaled scores before weighting the values.
